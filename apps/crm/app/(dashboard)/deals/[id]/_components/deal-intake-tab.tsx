import { IntakeEditorCard } from "./intake-editor-card";
import type { IntakeEditorCardProps } from "./intake-editor-card.types";

export function DealIntakeTab(props: IntakeEditorCardProps) {
  return <IntakeEditorCard {...props} />;
}
