{
  "name": "workers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "worker:all": "bun run src/main.ts all",
    "worker:ledger": "bun run src/main.ts ledger",
    "worker:documents": "bun run src/main.ts documents",
    "worker:balances": "bun run src/main.ts balances",
    "worker:fx-rates": "bun run src/main.ts fx-rates",
    "worker:reconciliation": "bun run src/main.ts reconciliation",
    "worker:connectors-dispatch": "bun run src/main.ts connectors-dispatch",
    "worker:connectors-poller": "bun run src/main.ts connectors-poller",
    "worker:connectors-statements": "bun run src/main.ts connectors-statements",
    "worker:orchestration-retry": "bun run src/main.ts orchestration-retry",
    "build": "tsgo",
    "check-types": "tsgo --noEmit",
    "lint": "eslint src/**/*.ts --max-warnings 0"
  },
  "dependencies": {
    "@bedrock/db": "workspace:*",
    "@bedrock/foundation": "workspace:*",
    "@bedrock/modules": "workspace:*",
    "@bedrock/platform": "workspace:*",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@bedrock/eslint-config": "workspace:*",
    "@bedrock/typescript-config": "workspace:*",
    "@types/node": "^24",
    "eslint": "^9.31.0",
    "typescript": "^5.8.3"
  }
}
