{
  "name": "workers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "worker:ledger": "bun run src/ledger.ts",
    "worker:transfers": "bun run src/transfers.ts",
    "worker:treasury": "bun run src/treasury.ts",
    "worker:reconciliation": "bun run src/reconciliation.ts",
    "worker:fx-rates": "bun run src/fx-rates.ts",
    "worker:all": "bun run src/all.ts",
    "build": "tsgo",
    "check-types": "tsgo --noEmit",
    "lint": "eslint src/**/*.ts --max-warnings 0"
  },
  "dependencies": {
    "@bedrock/currencies": "workspace:*",
    "@bedrock/db": "workspace:*",
    "@bedrock/fees": "workspace:*",
    "@bedrock/fx": "workspace:*",
    "@bedrock/kernel": "workspace:*",
    "@bedrock/ledger": "workspace:*",
    "@bedrock/transfers": "workspace:*",
    "@bedrock/treasury": "workspace:*",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@bedrock/typescript-config": "workspace:*",
    "@bedrock/eslint-config": "workspace:*",
    "@types/node": "^24",
    "typescript": "^5.8.3",
    "eslint": "^9.31.0"
  }
}
