{
  "name": "bedrock",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency 30",
    "lint": "turbo run lint",
    "check:boundaries": "node ./scripts/check-boundaries.mjs && node ./scripts/check-accounting-literals.mjs && node ./scripts/check-deprecated-imports.mjs",
    "check:workspace-deps": "node ./scripts/check-workspace-deps.mjs",
    "pack:validate": "bun run --filter=@bedrock/platform pack:validate",
    "pack:compile": "bun run --filter=@bedrock/platform pack:compile",
    "pack:activate": "bun run --filter=@bedrock/platform pack:activate",
    "check-types": "turbo run check-types",
    "test": "vitest run --project api --project workers --project countries --project db --project accounting --project balances --project connectors --project component-runtime --project counterparties --project currencies --project customers --project dimensions --project documents --project idempotency --project ledger --project operational-accounts --project orchestration --project reconciliation --project accounting-reporting --project fees --project fx --project payments",
    "test:watch": "vitest --project api --project workers --project countries --project db --project accounting --project balances --project connectors --project component-runtime --project counterparties --project currencies --project customers --project dimensions --project documents --project idempotency --project ledger --project operational-accounts --project orchestration --project reconciliation --project accounting-reporting --project fees --project fx --project payments",
    "test:coverage": "vitest run --coverage --project api --project workers --project countries --project db --project accounting --project balances --project connectors --project component-runtime --project counterparties --project currencies --project customers --project dimensions --project documents --project idempotency --project ledger --project operational-accounts --project orchestration --project reconciliation --project accounting-reporting --project fees --project fx --project payments",
    "test:integration": "vitest run --project accounting:integration --project balances:integration --project customers:integration --project fees:integration --project fx:integration --project ledger:integration --project reconciliation:integration",
    "test:integration:watch": "vitest --project accounting:integration --project balances:integration --project customers:integration --project fees:integration --project fx:integration --project ledger:integration --project reconciliation:integration",
    "test:all": "npm run test && npm run test:integration",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "knip": "knip"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@typescript/native-preview": "^7.0.0-dev.20260226.1",
    "@vitest/coverage-v8": "^4.0.18",
    "knip": "^5.85.0",
    "prettier": "^3.8.1",
    "turbo": "^2.8.12",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": "24.x"
  },
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/foundation",
    "packages/db",
    "packages/platform",
    "packages/modules",
    "packages/sdk/*",
    "packages/tooling/*"
  ]
}
