{
  "name": "multihansa-workspace",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency 15",
    "lint": "turbo run lint",
    "check:boundaries": "node ./scripts/check-boundaries.mjs && node ./scripts/check-accounting-literals.mjs && node ./scripts/check-deprecated-imports.mjs",
    "check:worker-runtime": "node ./scripts/check-worker-runtime.mjs",
    "check:workspace-deps": "node ./scripts/check-workspace-deps.mjs",
    "pack:validate": "bun run --filter=@multihansa/accounting pack:validate",
    "pack:compile": "bun run --filter=@multihansa/accounting pack:compile",
    "pack:activate": "bun run --filter=@multihansa/accounting pack:activate",
    "check-types": "turbo run check-types",
    "test": "vitest run --project multihansa-api --project multihansa-workers --project multihansa-db --project common --project identity --project assets --project ledger --project accounting --project balances --project reconciliation --project documents --project parties --project treasury --project reporting",
    "test:watch": "vitest --project multihansa-api --project multihansa-workers --project multihansa-db --project common --project identity --project assets --project ledger --project accounting --project balances --project reconciliation --project documents --project parties --project treasury --project reporting",
    "test:coverage": "vitest run --coverage --project multihansa-api --project multihansa-workers --project multihansa-db --project common --project identity --project assets --project ledger --project accounting --project balances --project reconciliation --project documents --project parties --project treasury --project reporting",
    "test:integration": "vitest run --passWithNoTests --project accounting:integration --project balances:integration --project customers:integration --project fees:integration --project fx:integration --project ledger:integration --project reconciliation:integration",
    "test:integration:watch": "vitest --passWithNoTests --project accounting:integration --project balances:integration --project customers:integration --project fees:integration --project fx:integration --project ledger:integration --project reconciliation:integration",
    "test:all": "npm run test && npm run test:integration",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "knip": "knip"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@next/eslint-plugin-next": "^16.1.6",
    "@types/node": "^25.3.0",
    "@typescript/native-preview": "^7.0.0-dev.20260226.1",
    "@vitest/coverage-v8": "^4.0.18",
    "eslint": "^10.0.3",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-only-warn": "^1.2.1",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-security": "^4.0.0",
    "eslint-plugin-turbo": "^2.8.15",
    "globals": "^17.4.0",
    "knip": "^5.85.0",
    "prettier": "^3.8.1",
    "turbo": "^2.8.12",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.57.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": "24.x"
  },
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/common",
    "packages/domains/identity",
    "packages/domains/assets",
    "packages/domains/ledger",
    "packages/domains/accounting",
    "packages/domains/balances",
    "packages/domains/reconciliation",
    "packages/domains/documents",
    "packages/domains/parties",
    "packages/domains/treasury",
    "packages/domains/reporting",
    "packages/domains/multihansa-app",
    "packages/db",
    "packages/ui"
  ]
}
