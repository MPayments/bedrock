{
  "name": "bedrock",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency 20",
    "lint": "turbo run lint",
    "check:boundaries": "node ./scripts/check-boundaries.mjs",
    "check:workspace-deps": "node ./scripts/check-workspace-deps.mjs",
    "check-types": "turbo run check-types",
    "test": "vitest run --project db --project countries --project customers --project currencies --project fees --project fx --project ledger --project treasury --project transfers --project counterparties",
    "test:watch": "vitest --project db --project countries --project customers --project currencies --project fees --project fx --project ledger --project treasury --project transfers --project counterparties",
    "test:coverage": "vitest run --coverage --project db --project countries --project customers --project currencies --project fees --project fx --project ledger --project treasury --project transfers --project counterparties",
    "test:integration": "vitest run --project customers:integration --project fees:integration --project fx:integration --project ledger:integration --project treasury:integration",
    "test:integration:watch": "vitest --project customers:integration --project fees:integration --project fx:integration --project ledger:integration --project treasury:integration",
    "test:all": "npm run test && npm run test:integration",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "knip": "knip"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@typescript/native-preview": "^7.0.0-dev.20260226.1",
    "@vitest/coverage-v8": "^4.0.18",
    "dependency-cruiser": "^17.3.8",
    "knip": "^5.85.0",
    "prettier": "^3.8.1",
    "turbo": "^2.8.11",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": "24.x"
  },
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/modules/*"
  ]
}
