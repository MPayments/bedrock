{
  "name": "bedrock",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency 15",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "test": "vitest run --project db --project fees --project fx --project ledger --project treasury --project transfers",
    "test:watch": "vitest --project db --project fees --project fx --project ledger --project treasury --project transfers",
    "test:coverage": "vitest run --coverage --project db --project fees --project fx --project ledger --project treasury --project transfers",
    "test:integration": "vitest run --project fees:integration --project ledger:integration --project treasury:integration",
    "test:integration:watch": "vitest --project fees:integration --project ledger:integration --project treasury:integration",
    "test:all": "npm run test && npm run test:integration",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "prettier": "^3.6.2",
    "turbo": "^2.8.6",
    "typescript": "5.8.3",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": "24.x"
  },
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/modules/*"
  ]
}
