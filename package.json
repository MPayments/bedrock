{
  "name": "bedrock",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency 10",
    "lint": "turbo run lint",
    "check:boundaries": "node ./scripts/check-boundaries.mjs && node ./scripts/check-accounting-literals.mjs && node ./scripts/check-deprecated-imports.mjs",
    "check:worker-runtime": "node ./scripts/check-worker-runtime.mjs",
    "check:workspace-deps": "node ./scripts/check-workspace-deps.mjs",
    "check:graph:build": "turbo run build --dry",
    "check:graph:types": "turbo run check-types --dry",
    "check:graph": "bun run check:graph:build && bun run check:graph:types",
    "pack:validate": "bun run --filter=@bedrock/accounting pack:validate",
    "pack:compile": "bun run --filter=@bedrock/accounting pack:compile",
    "pack:activate": "bun run --filter=@bedrock/accounting pack:activate",
    "check-types": "turbo run check-types",
    "test": "vitest run --project api --project workers --project kernel --project db --project accounting --project accounting-reporting --project auth --project balances --project commercial-documents --project currencies --project documents --project fees --project fx --project idempotency --project ifrs-documents --project ledger --project parties --project parties-ledger --project reconciliation --project users --project worker-runtime",
    "test:watch": "vitest --project api --project workers --project kernel --project db --project accounting --project accounting-reporting --project auth --project balances --project commercial-documents --project currencies --project documents --project fees --project fx --project idempotency --project ifrs-documents --project ledger --project parties --project parties-ledger --project reconciliation --project users --project worker-runtime",
    "test:coverage": "vitest run --coverage --project api --project workers --project kernel --project db --project accounting --project accounting-reporting --project auth --project balances --project commercial-documents --project currencies --project documents --project fees --project fx --project idempotency --project ifrs-documents --project ledger --project parties --project parties-ledger --project reconciliation --project users --project worker-runtime",
    "test:integration": "vitest run --project accounting:integration --project balances:integration --project fees:integration --project fx:integration --project ledger:integration --project parties:integration --project reconciliation:integration",
    "test:integration:watch": "vitest --project accounting:integration --project balances:integration --project fees:integration --project fx:integration --project ledger:integration --project parties:integration --project reconciliation:integration",
    "test:all": "bun run test && bun run test:integration",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "knip": "knip"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@types/pg": "^8.16.0",
    "@typescript/native-preview": "^7.0.0-dev.20260226.1",
    "@vitest/coverage-v8": "^4.0.18",
    "knip": "^5.85.0",
    "pg": "^8.18.0",
    "prettier": "^3.8.1",
    "turbo": "^2.8.12",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": "24.x"
  },
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/kernel",
    "packages/modules/*",
    "packages/platform/*",
    "packages/runtime/*",
    "packages/plugins/*",
    "packages/integrations/*",
    "packages/sdk/*",
    "packages/tooling/*"
  ]
}
