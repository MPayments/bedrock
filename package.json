{
  "name": "bedrock",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "db:generate": "bun run --filter=db db:generate",
    "db:migrate": "bun run --filter=db db:migrate",
    "db:nuke": "bun run --filter=db db:nuke",
    "db:push": "bun run --filter=db db:push",
    "db:seed": "bun run --filter=db db:seed",
    "db:seed:accounting": "bun run --filter=db db:seed:accounting",
    "db:bootstrap:accounting": "bun run --filter=db db:bootstrap:accounting",
    "db:seed:users": "bun run --filter=db db:seed:users",
    "db:seed:counterparties": "bun run --filter=db db:seed:counterparties",
    "db:seed:organizations": "bun run --filter=db db:seed:organizations",
    "db:seed:requisite-providers": "bun run --filter=db db:seed:requisite-providers",
    "db:seed:requisites": "bun run --filter=db db:seed:requisites",
    "db:studio": "bun run --filter=db db:studio",
    "dev": "turbo run dev --concurrency 100",
    "lint": "turbo run lint",
    "check:boundaries": "node ./scripts/check-boundaries.mjs && node ./scripts/check-accounting-literals.mjs && node ./scripts/check-deprecated-imports.mjs && node ./scripts/check-schema-entrypoints.mjs",
    "check:worker-runtime": "node ./scripts/check-worker-runtime.mjs",
    "check:cycles": "node ./scripts/check-workspace-cycles.mjs",
    "check:workspace-deps": "node ./scripts/check-workspace-deps.mjs",
    "check:graph:build": "turbo run build --dry",
    "check:graph:types": "turbo run check-types --dry",
    "check:graph": "bun run check:cycles && bun run check:graph:build && bun run check:graph:types",
    "pack:validate": "bun run --filter=@bedrock/accounting pack:validate",
    "pack:compile": "bun run --filter=@bedrock/accounting pack:compile",
    "pack:activate": "bun run --filter=@bedrock/accounting pack:activate",
    "check-types": "turbo run check-types",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "test:coverage": "vitest run --coverage --config vitest.config.ts",
    "test:integration": "vitest run --config vitest.integration.config.ts --project accounting:integration --project balances:integration --project parties:integration --project fees:integration --project fx:integration --project ledger:integration --project requisites:integration --project reconciliation:integration",
    "test:integration:watch": "vitest --config vitest.integration.config.ts --project accounting:integration --project balances:integration --project parties:integration --project fees:integration --project fx:integration --project ledger:integration --project requisites:integration --project reconciliation:integration",
    "test:all": "bun run test && bun run test:integration",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "knip": "knip"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@types/pg": "^8.16.0",
    "@typescript/native-preview": "^7.0.0-dev.20260226.1",
    "@vitest/coverage-v8": "^4.0.18",
    "knip": "^5.85.0",
    "pg": "^8.18.0",
    "prettier": "^3.8.1",
    "turbo": "^2.8.16",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": "24.x"
  },
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/shared",
    "packages/modules/*",
    "packages/platform",
    "packages/plugins/*",
    "packages/sdk/*",
    "packages/workflows/*",
    "packages/tooling/*"
  ],
  "patchedDependencies": {
    "tlsclientwrapper@4.1.2": "patches/tlsclientwrapper@4.1.2.patch"
  }
}
