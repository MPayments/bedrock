import {
  documentEvents,
  documentLinks,
  documentOperations,
  documents,
  documentSnapshots,
  schema as documentsSchema,
} from "./documents/adapters/drizzle/schema";

export {
  documentEvents,
  documentLinks,
  documentOperations,
  documents,
  documentSnapshots,
};

export const schema = documentsSchema;
