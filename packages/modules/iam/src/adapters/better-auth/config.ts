import {
  account,
  session,
  twoFactor,
  user,
  verification,
} from "../drizzle/schema/auth-schema";

export const betterAuthSchema = {
  account,
  session,
  twoFactor,
  user,
  verification,
};
