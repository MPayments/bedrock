{
  "name": "@bedrock/workflow-period-close",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc --watch -p tsconfig.json",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\" \"tests/**/*.ts\" --no-error-on-unmatched-pattern --max-warnings 0",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts"
  },
  "dependencies": {
    "@bedrock/accounting-close": "workspace:*",
    "@bedrock/adapter-db-drizzle": "workspace:*",
    "@bedrock/adapter-worker-runtime": "workspace:*",
    "@bedrock/counterparties": "workspace:*",
    "@bedrock/core": "workspace:*",
    "@bedrock/documents": "workspace:*",
    "@bedrock/identity": "workspace:*",
    "@bedrock/observability": "workspace:*",
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "@bedrock/test-utils": "workspace:*",
    "@bedrock/typescript-config": "workspace:*",
    "@types/node": "^24",
    "eslint": "^9.31.0",
    "typescript": "^5.8.3",
    "vitest": "^4.0.18"
  },
  "bedrock": {
    "kind": "workflow",
    "tags": [
      "workflow",
      "period-close"
    ]
  }
}
