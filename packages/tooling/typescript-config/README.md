# @multihansa/typescript-config

Shared TypeScript configuration package for the monorepo.

## Purpose

- Central place for reusable TS config presets.
- Keeps compiler options aligned across apps and packages.

## Usage

Packages extend configs from this package in their local `tsconfig.json`.

## Package notes

- This package contains configuration only.
- No runtime code is exported.

