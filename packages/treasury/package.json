{
    "name": "@repo/treasury",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "exports": {
        ".": "./src/index.ts"
    },
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "check-types": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:coverage": "vitest run --coverage"
    },
    "dependencies": {
        "@repo/db": "*",
        "@repo/kernel": "*",
        "@repo/ledger": "*",
        "drizzle-orm": "^0.45.1",
        "zod": "^4.3.6"
    },
    "devDependencies": {
        "@repo/test-utils": "*",
        "@repo/typescript-config": "*",
        "typescript": "^5.8.3",
        "vitest": "^4.0.18"
    }
}