{
  "name": "@repo/ledger",
  "version": "0.0.0",
  "private": true,
  "type": "bundler",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@repo/kernel": "*",
    "@repo/db": "*",
    "drizzle-orm": "^0.45.1",
    "tigerbeetle-node": "^0.16.11"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "@vitest/coverage-v8": "^3.1.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
